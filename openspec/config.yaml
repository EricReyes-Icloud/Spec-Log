# OpenSpec Configuration — spec-log
# Generated by sdd-init

schema: spec-driven

context: |
  Tech stack: Next.js 16.2.6, React 19.2.4, TypeScript 5.x (strict), Tailwind CSS v4
  Architecture: Modular (app/, components/email/, emails/templates/, lib/services/, styles/)
  Email: React Email + Resend
  Persistence: Supabase
  Hosting: Vercel

persistence:
  mode: openspec

testing:
  strict_tdd: false
  reason: No test runner found — no jest/vitest/playwright in package.json, no test scripts defined, no testing config files
  test_runner: none
  coverage: none

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas